name: Publish

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Existing Git tag / GitHub Release to publish
        required: true
        type: string
      targets:
        description: Store targets to submit
        required: true
        type: choice
        options:
          - chrome
          - firefox
          - both
      dry_run:
        description: Run Firefox AMO submission in dry-run mode
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  submit-chrome:
    if: ${{ inputs.targets == 'chrome' || inputs.targets == 'both' }}
    runs-on: ubuntu-latest
    environment: chrome-web-store
    env:
      RELEASE_TAG: ${{ inputs.release_tag }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          package-manager-cache: false
      - run: corepack enable
      - run: pnpm install --frozen-lockfile
      - run: pnpm validate:ids
      - name: Download release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          [[ "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          mkdir -p dist
          gh release download "$RELEASE_TAG" --dir dist --pattern chrome.zip --pattern SHA256SUMS.txt
      - name: Verify checksums
        run: |
          set -euo pipefail
          grep '  chrome.zip$' dist/SHA256SUMS.txt > dist/chrome.SHA256SUMS.txt
          test "$(wc -l < dist/chrome.SHA256SUMS.txt)" -eq 1
          (cd dist && sha256sum -c chrome.SHA256SUMS.txt)
      - name: Submit to Chrome Web Store
        env:
          CHROME_EXTENSION_ID: ${{ secrets.CHROME_EXTENSION_ID }}
          CHROME_CLIENT_ID: ${{ secrets.CHROME_CLIENT_ID }}
          CHROME_CLIENT_SECRET: ${{ secrets.CHROME_CLIENT_SECRET }}
          CHROME_REFRESH_TOKEN: ${{ secrets.CHROME_REFRESH_TOKEN }}
        run: |
          set -euo pipefail
          REPO_ROOT="$PWD"
          cd packages/extension
          pnpm wxt submit \
            --chrome-zip "$REPO_ROOT/dist/chrome.zip" \
            2>&1 | tee "$REPO_ROOT/dist/chrome-submit.log"
      - name: Write workflow summary
        run: |
          {
            echo "## Chrome Web Store Submission"
            echo
            echo "Release tag: \`$RELEASE_TAG\`"
            echo
            echo '```'
            cat dist/chrome-submit.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

  submit-firefox:
    if: ${{ inputs.targets == 'firefox' || inputs.targets == 'both' }}
    runs-on: ubuntu-latest
    environment: firefox-amo
    env:
      RELEASE_TAG: ${{ inputs.release_tag }}
      DRY_RUN: ${{ inputs.dry_run }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          package-manager-cache: false
      - run: corepack enable
      - run: pnpm install --frozen-lockfile
      - run: pnpm validate:ids
      - name: Download release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          [[ "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]
          mkdir -p dist/firefox-unpacked
          gh release download "$RELEASE_TAG" --dir dist --pattern firefox.zip --pattern firefox-sources.zip --pattern SHA256SUMS.txt
          unzip -q dist/firefox.zip -d dist/firefox-unpacked
      - name: Verify checksums
        run: |
          set -euo pipefail
          grep -E '  (firefox.zip|firefox-sources.zip)$' dist/SHA256SUMS.txt > dist/firefox.SHA256SUMS.txt
          test "$(wc -l < dist/firefox.SHA256SUMS.txt)" -eq 2
          (cd dist && sha256sum -c firefox.SHA256SUMS.txt)
      - name: Validate Firefox publish gate
        run: |
          pnpm validate:firefox-publish -- \
            --build-dir dist/firefox-unpacked \
            --firefox-zip dist/firefox.zip \
            --firefox-sources-zip dist/firefox-sources.zip
      - name: Submit to AMO
        env:
          FIREFOX_EXTENSION_ID: ${{ secrets.FIREFOX_EXTENSION_ID }}
          FIREFOX_JWT_ISSUER: ${{ secrets.FIREFOX_JWT_ISSUER }}
          FIREFOX_JWT_SECRET: ${{ secrets.FIREFOX_JWT_SECRET }}
        run: |
          set -euo pipefail
          REPO_ROOT="$PWD"
          cd packages/extension
          submit_args=(
            --firefox-zip "$REPO_ROOT/dist/firefox.zip"
            --firefox-sources-zip "$REPO_ROOT/dist/firefox-sources.zip"
            --firefox-channel listed
          )
          if [[ "$DRY_RUN" == "true" ]]; then
            submit_args+=(--dry-run)
          fi
          pnpm wxt submit \
            "${submit_args[@]}" \
            2>&1 | tee "$REPO_ROOT/dist/firefox-submit.log"
      - name: Write workflow summary
        run: |
          {
            echo "## Firefox AMO Submission"
            echo
            echo "Release tag: \`$RELEASE_TAG\`"
            echo "Dry run: \`$DRY_RUN\`"
            echo
            echo '```'
            cat dist/firefox-submit.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
